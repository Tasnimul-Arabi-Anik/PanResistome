# Test Fixtures

`local_samples/` contains tiny prebuilt sample directories used by the `test`
Nextflow profile. These fixtures let CI exercise sequence QC, metadata
enrichment, pass-only metadata generation, and result collection without NCBI
downloads, Conda environments, CheckM2 databases, ABRicate databases, or GTDB-Tk
reference data.
